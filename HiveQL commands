hive

show databases;  #to see databases
create database db;
use db;

drop database db;

create database test;
use test;
CREATE TABLE employee (
  id INT,
  name STRING,
  salary FLOAT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ','
STORED AS TEXTFILE;

alter table student add columns (city varchar(10));
alter table student rename to employee;

drop table student;

load data local inpath 'path/to/employee.txt' into table employee;

create external table logs(
user_id int,
user_name varchar(10)) row format delimited
fields terminated by '\t
stored as textfile
location '/external path'


create table table1(
empId int,
emp_name varchar(10),
salary int);

create table table2(
empId int,
dept varchar(10));



CREATE EXTERNAL TABLE IF NOT EXISTS logs (
  userId INT,
  userName VARCHAR(16)
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\t'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION 'hdfs://quickstart.cloudera:8020/user/cloudera/logs/extra';






